/* Build-time About-page sync (Phase 3 — placeholder).
 *
 * Will pull the About ("Our Story") content from a Wix CMS collection on
 * "KORA Retail" and write src/data/about.json, so OurStory.tsx renders editable
 * content. Until the CMS collection + wiring land, this is a graceful no-op so
 * the build pipeline stays intact.
 */
console.log('[sync-about] placeholder — About CMS wiring pending (Phase 3). Skipping.');
process.exit(0);
